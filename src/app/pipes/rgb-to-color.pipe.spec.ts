import { RgbToColorPipe } from './rgb-to-color.pipe';

describe('RgbToColorPipe', () => {
  it('create an instance', () => {
    const pipe = new RgbToColorPipe();
    expect(pipe).toBeTruthy();
  });
});
